<input type="{{type}}"
       placeholder="{{placeHolder}}"
       name="{{id}}"
       id="{{id}}"
       autocomplete="{{autocomplete}}"
       required={{required}}
       accept="{{accept}}"
/>
<label for="{{id}}"></label>
